class User {
    int goldCoins;
    ArrayList<Note> notes;
}

void createNote() {
    notes.add(new Note);
}

void storage(String fileName, int numTabs) {
    String[] noteContent = loadStrings(fileName);
}
